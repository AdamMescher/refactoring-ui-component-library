import React from 'react';
import StyledButton from './styled';

const Button = (
  text,
  size,
  borderRadius,
  gradient,
  raised,
  icon,
) => (
  <StyledButton size={size} borderRadius={}>{text}</StyledButton>
);

export default Button;
